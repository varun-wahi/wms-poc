# Copy this file to wms_agent/.env (ADK auto-loads it)

# Option A: Using Gemini API Key (No GCP account needed)
GEMINI_API_KEY=your_gcp_api_key_here

# Option B: Using GCP Vertex AI (Uncomment if using Vertex AI)
# GOOGLE_GENAI_USE_VERTEXAI=TRUE
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_CLOUD_LOCATION=us-central1

MODEL=gemini-3.6-flash

# Database Configuration (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wms
DB_USER=postgres
DB_PASSWORD=postgres

# curl "https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent" \
#   -H 'Content-Type: application/json' \
#   -H 'X-goog-api-key: your_gcp_api_key_here' \
#   -X POST \
#   -d '{
#     "contents": [
#       {
#         "parts": [
#           {
#             "text": "Explain how AI works in a few words"
#           }
#         ]
#       }
#     ]
#   }'